//timestamp locally, check this,
//go to the 'sync' table in firestore and retrieve all records with a timestamp => local timestamp
//loop through these records, find each record and copy it to the local table



//as you update the firestore database add a new sync record, 
//for each record you change or add
//sync table 
    //timestamp
    //tablename
    //record id



//devices, on start if first time, download all online data
//